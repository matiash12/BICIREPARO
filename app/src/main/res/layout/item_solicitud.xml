<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:layout_marginHorizontal="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp">

        <androidx.cardview.widget.CardView
            android:layout_width="80dp"
            android:layout_height="80dp"
            app:cardCornerRadius="8dp"
            app:cardElevation="0dp"
            android:layout_marginEnd="12dp">

            <ImageView
                android:id="@+id/solicitudImageView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/ic_popular"
                android:background="#F0F0F0"
                tools:ignore="ContentDescription" />
        </androidx.cardview.widget.CardView>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/solicitudDescriptionTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Descripción de la solicitud"
                android:textSize="16sp"
                android:textColor="@color/bike_grey"
                android:maxLines="6"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/solicitudDateTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Fecha: 01/01/2025"
                android:textSize="12sp"
                android:textColor="#757575"
                android:layout_marginTop="8dp" />
        </LinearLayout>

        <ImageView
            android:id="@+id/solicitudDeleteButton"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            app:tint="@color/bike_grey"
            android:layout_marginStart="8dp"
            android:contentDescription="Eliminar Solicitud" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>